# Pooling

[API](https://github.com/markdibarry/Pooling/blob/main/docs/GameCore.Pooling.md)

Provides static classes for pooling objects, Lists, and scenes.

Uses the GameCore.SceneHelpers package to generate static scene paths for custom Godot classes, and adds an `IScene` interface.
